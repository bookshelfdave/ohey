#!/usr/bin/env ruby
#
require 'readline'
require 'json'
require 'ohey'
puts "Oh, hey!"

file = open(ARGV[0])
json = file.read
parsed = JSON.parse(json)

while line = Readline.readline('> ', true)
  puts line
end
